import { ContainerPropsType } from "../types/components-types";
import { FieldType } from "../types/field-types";

export const ContainerFormFields: Record<keyof ContainerPropsType, FieldType> =
  {
    name: "text",
  };
